{
  "name": "commend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write .",
    "start:firefox": "web-ext run",
    "dev:firefox": "web-ext run --reload",
    "build:firefox": "web-ext build --overwrite-dest",
    "start:chrome": "web-ext run --target chromium",
    "dev:chrome": "web-ext run --reload --target chromium"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "web-ext": "^7.11.0"
  }
}
